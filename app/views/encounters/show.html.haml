- content_for(:subheader) do
  .row.center.edit
    .col.pe-10
      .row
        .col
          %h2
            = link_to "javascript:history.back();", class: 'side-link', title: I18n.t('views.back') do
              = image_tag "arrow-left.png"
            Test Order
= react_component('EncounterShow', encounter: @encounter_as_json,  requested_tests: @encounter.requested_tests, requested_by:   @encounter.user !=nil ? @encounter.user.email : '', can_update: @can_update, show_edit: @show_edit_encounter, show_cancel: @show_cancel_encounter, referer: @return_path_encounter, status_types:RequestedTest.statuses, associated_tests_to_results: @associated_tests_to_results)
