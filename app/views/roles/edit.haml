- content_for(:subheader) do
  .row.center.add-new
    .col.pe-10
      .row
        .col
          %h2
            = link_to roles_path, class: 'side-link', title: I18n.t('views.back') do
              = image_tag "arrow-left-white.png"
            = @role.name

= render 'form'
