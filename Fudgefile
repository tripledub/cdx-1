task_group :rspec_cdx_core do
  in_directory 'cdx_core' do
    shell 'bundle' unless ENV['USER'] == 'travis'
    shell 'bundle exec rspec --exclude-pattern "**/features/*_spec.rb"'
  end
end

build :default do
  task_group :rspec_cdx_core
end
