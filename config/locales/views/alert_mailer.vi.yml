vi:
  alert_mailer:
    alert_email:
      h1: 'Thông báo sự cố cho #{@alert.name}'
      msg: "Lúc #{@alert.created_at.strftime('%m/%d/%Y %H:%M')}"
      email_text: 'Cảnh báo xảy ra #{@alert.name}. Thời gian xảy ra #{@alert.created_at}. #{@alert_body_text}'