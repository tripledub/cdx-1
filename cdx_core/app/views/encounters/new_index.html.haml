- content_for(:subheader) do
  .row.center.add-new.newtestoptions.collapse
    .col.pe-10
      %h2
        = link_to test_orders_path, class: 'side-link', title: I18n.t('views.back') do
          = image_tag "arrow-left-white.png"
        New Tests


.row
  .optionsarea
    %header 
      %h3
        Please select one of the options below.
    %ul.nav-list
      %li{'data-href' => new_encounter_path(mode: 'fresh_tests')}
        %span New test order
        .text-small.muted
          %i Order diagnostic tests against patients and samples. Specify sample type, test type, reason for examination and due date. Add sample IDs or use the sample ID generator.
      %li{'data-href' => new_encounter_path(mode: 'existing_tests')}
        %span Already reported tests
        .text-small.muted
          %i Organise and assign test results to patients. Review results and provide diagnosis.
